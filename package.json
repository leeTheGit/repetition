{
  "name": "repetition",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -F=frontend dev",
    "build": "pnpm -F=frontend build",
    "typecheck": "tsc --noEmit",
    "db:seed": "pnpm -F=core db:seed",
    "migrate": "pnpm -F=core migrate",
    "deploy:lambda": "pnpm -F=infra deploy:lambda",
    "deploy:all": "pnpm -F=infra deploy",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.16.2",
    "typescript": "^5",
    "vitest": "^2.1.2"
  },
  "dependencies": {
    "sst": "ion",
    "ts-node": "^10.9.2",
    "zod": "^3.23.8"
  }
}